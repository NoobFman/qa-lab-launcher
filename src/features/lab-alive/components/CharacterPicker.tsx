import{characters}from'../config/characters';import type{CharacterId}from'../types';
export function CharacterPicker({onSelect,onClose,closable=false}:{onSelect:(id:CharacterId)=>void;onClose?:()=>void;closable?:boolean}){return <div className="aliveModalBackdrop"><section className="aliveModal" role="dialog" aria-modal="true" aria-labelledby="who-title"><div className="modalHeading"><div><span className="eyebrow">QA LAB ALIVE</span><h2 id="who-title">WHO ARE YOU?</h2><p>Scegli il tuo avatar. Tutti continueranno a vivere nel Lab.</p></div>{closable&&<button className="modalClose" onClick={onClose}>×</button>}</div><div className="characterGrid">{characters.map(c=><button key={c.id} onClick={()=>onSelect(c.id)} className="characterChoice"><span className="choiceAvatar" style={{'--skin':c.appearance.skin,'--hair':c.appearance.hair,'--shirt':c.appearance.shirt} as React.CSSProperties}><i/><b/></span><strong>{c.name}</strong><small>{c.signature}</small></button>)}</div></section></div>}
